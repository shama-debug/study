# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=todo_db

# Application
APP_PORT=8080
GIN_MODE=debug

# Create a long random value, e.g. `openssl rand -base64 32`.
JWT_SECRET=replace-with-a-long-random-secret
